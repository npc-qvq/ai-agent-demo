# AI 通用代码规则

## 回复要求

- 使用中文回复。
  - 回答要直接，优先给可落地的修改方案和代码。

## 注释规范

- 编写 Java 类时，类定义必须添加 JavaDoc 注释，说明该类的业务用途。
  - 类中的字段必须添加 JavaDoc 注释，说明字段含义。
  - 字段的 JavaDoc 必须使用多行格式，禁止单行格式 `/** ... */`。
  - 编写接口时，接口本身必须添加 JavaDoc 注释，说明接口用途。
  - 接口中的方法必须添加 JavaDoc 注释，说明方法用途、入参和出参。
  - 接口实现类中，如果定义方法，必须在方法上方添加 JavaDoc 注释，说明方法用途、入参和出参。
  - 常量必须添加 JavaDoc 注释，说明常量含义。
  - 注释应直接说明代码含义或业务用途，避免空泛描述。
  - 代码注释不要使用 `1、2、3` 这类步骤式注释。

## 基本原则

- 不要随意大范围重构，除非我明确要求写一个新类或重构。
  - 不要删除已有业务逻辑。
  - 不要删除已有注释。
  - 修改代码只改当前问题相关部分。
  - 不确定业务含义时，先保守处理，不要猜业务。
  - 生成代码时优先保证可读性，不要为了简短牺牲清晰度。

## Java 代码风格

- 使用清晰的普通写法。
  - 优先使用 JDK 1.8 可用的语法。
  - 遍历集合时优先使用 `forEach`，优先保证可读性。
  - 不要随意改变已有方法入参、返回值和调用链。
  - 所有 `if` 语句必须加大括号。
  - 写逻辑时先判空，再执行业务处理。
  - 避免多层嵌套的阶梯式代码。
  - 可以通过提前返回减少嵌套。
  - 方法返回值为集合时，优先返回空集合，不要返回 `null`，除非原有代码约定就是返回 `null`。
  - 方法返回值为对象时，如果无法构造有效结果，可以返回 `null`，但调用方使用前必须判空。
  - 不要为了简化代码引入复杂写法。